import GalleryClient from './GalleryClient';

export const metadata = {
  title: "Yapılan İşler & Galeri | Emira Motors",
  description: "Engin Usta'nın dükkanında yapılan motor tamirleri, şasi kaynakları ve ağır bakımların öncesi-sonrası karşılaştırmaları ve işçilik galerisi.",
};

export default function GalleryPage() {
  return <GalleryClient />;
}
